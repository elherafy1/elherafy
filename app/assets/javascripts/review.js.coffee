var ch = document.getElementsByName('ratingA');
for (var i = ch.length; i--;) {
    ch[i].onchange = function() {
       // alert(this.value);
        document.write(this.value);
      
    }
}
var ch = document.getElementsByName('ratingB');
for (var i = ch.length; i--;) {
    ch[i].onchange = function() {
       // alert(this.value);
        document.write(this.value);
      
    }
}
var ch = document.getElementsByName('ratingC');
for (var i = ch.length; i--;) {
    ch[i].onchange = function() {
       // alert(this.value);
        document.write(this.value);
      
    }
}
var ch = document.getElementsByName('ratingD');
for (var i = ch.length; i--;) {
    ch[i].onchange = function() {
       // alert(this.value);
        document.write(this.value);
      
    }
}
var ch = document.getElementsByName('ratingE');
for (var i = ch.length; i--;) {
    ch[i].onchange = function() {
       // alert(this.value);
        document.write(this.value);
      
    }
}